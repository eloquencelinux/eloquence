[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "eloquence-os"
version = "2026.1.0"
description = "Eloquence GNU/Linux distribution configuration and live build tooling"
readme = "README.md"
requires-python = ">=3.10"
